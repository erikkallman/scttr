#ifndef QUICKSORT_H
#define QUICKSORT_H

void
quicksort(double * x, int * sorted_idx,
               int first, int last, int n_el);

void
quicksort_d(double * x, double * sorted_idx,
           int first, int last, int n_el);

#endif /* QUICKSORT_H */
