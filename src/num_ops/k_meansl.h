#ifndef K_MEANSL_H
#define K_MEANSL_H

/* function k_meansl
   k_meansl expects an array @a of values in inreasing values.
   * synopsis:

   * algorithm:

   * input:

   * output:

   * side-effects:

   */
int
k_meansl (double * a,
          int ** groups,
          int n_vals);

#endif /* K_MEANSL_H */
